import { NextPage } from "next";
import { ReactElement } from "react";
import { SignupModule } from "./_modules";

const SignupPage: NextPage = (): ReactElement => {
  return <SignupModule />;
};

export default SignupPage;
