import { NextPage } from "next";
import { ReactElement } from "react";
import { MainModule } from "./_modules";

const MainPage: NextPage = (): ReactElement => {
  return <MainModule />;
};

export default MainPage;
